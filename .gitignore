# Caches de herramientas
.atl/.skill-registry.cache.json
# Auto-generated (machine-specific absolute paths + churning timestamp)
.atl/skill-registry.md

# Python
__pycache__/
*.pyc

# Temporales
*.tmp
*.bak

# detect-tools.sh capability-report cache (written to CWD by default;
# DEPENDENCIES.md instructs running the script, so it appears anywhere)
.research-tools.txt
