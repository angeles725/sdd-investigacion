# ODD — kit audit and open items, 2026-09-23

## Objective
Continue the 2026-09-22/23 kit work (resume #912, #933, #936 and #940), audit `/research-sdd` for
weak spots, and fix what the audit finds, delivering one PR per work unit.

## Problem / why
The previous session closed four units unfinished. An independent audit was requested to find
false-confidence channels, doctrine↔code drift and security gaps that remained after those
sessions.

## Authorized scope
Maintainer (2026-09-23): "ODD + RDD (granted always), commit + push + PR + pr view + issues +
merge", full autonomy ("no me preguntes nada … tienes permitido todo"). Other decisions:
- OpenCode support is dropped from now on (#954).
- #942: `med` stays INVALID_PRIORITY and the corpus is fixed instead.
- The PR-level review uses Opus, never Fable.

## Constraints
- CLAUDE.md §3 disjoint writer file sets, §4 executed RED + real teeth, §6 doctrine first,
  §7 anti-silent-zero, §8 propose-never-apply.
- Target corpora change only through explicit maintainer-authorized commits.

## Routes and models
- Every unit uses delegated direct routing (writer trigger: 2+ non-trivial files). One sonnet writer per unit
  runs in a harness worktree (the `default` model assignment).
- Native RDD 4R runs per PR, with consent pre-granted, followed by a PR-level adversarial review with Opus.
- Writer claims are re-run by the parent before review.
- TDD: strict (CLAUDE.md §4). Runner: `research-sdd/toolbelt/tests/*.test.sh`
  and `run-all.sh --prove-teeth`.

## Tasks
- [x] T1 — #942 decided: keep `med` invalid. niagara-research `00f1a43fb` normalizes 14 cells
  (not 9: the SES and SMA tables carry priority in column 2). #942 closed.
- [x] T2 — Stray `--sync-state` writes in niagara from the previous session were restored (the diff is backed
  up in the session scratchpad). Writers must never run `--sync-state` on live corpora.
- [x] T3 — Audit (Opus, read-only): 13 findings. Filed #955-#965; evidence added to #943 and #903.
- [x] T4 — #940 retro-gate follow-ups: PR #953 merged as `7cd46e0`. Advisories are in #971.
- [x] T5 — #958 verify-registry per-row absent targets and a non-silent sweep-tools-hook:
  PR #968 merged as `3f9b15b`.
- [x] T6 — #936 verify-skill-drift follow-ups, OpenCode parts dropped: PR #952 merged as `5bb6d37`.
- [x] T7 — #956 verify-block `resolved N of M`, with the pipefail race fixed in P6/P9: PR #972 merged
  as `bf0c68f`. Full-fleet sweep: 1,625 blocks, 0 exit flips, 0 WARN on resolving blocks.
  Follow-up #973.
- [x] T8 — #964 stage-retro branches from origin/main with `--no-track`; fetch failure is
  degraded: PR #975 merged as `5e28a1a`. Follow-up #976.
- [x] T9 — #912 zero-delta honesty line: PR #966 merged as `8b42201`. The whole PR exceeded the RDD
  lens context budget, so each of its 6 commits was reviewed as its own candidate: 5 approved and
  acknowledged, 1 docs slice passive. All 60 adversarial probes behave as expected; the suites pass
  under mawk. niagara retro fixed in `6c871d054`. The ford retro still carries the fake row: the edit
  was denied because ford is not a working directory. Advisories are in #979.
- [ ] T10 — #955 ensure-remote scans committed content and history: **PR #969 left OPEN** (handoff comment
  on the PR). There were 3 rounds. Round 3 (`b021e0d`) fixes the 5 blockers and 2 majors of the second Opus security review:
  `git log --raw -z` (blob, path) enumeration, checked cat-file/mktemp/pipelines, `-a`, NUL-safe paths,
  `--no-replace-objects`, `--no-follow-tags`. Native RDD approved and acknowledged. The final Opus security
  re-review was interrupted by a session restart, so the next session must re-run it before merging (repro harness in
  the session scratchpad `rev969b`; it is lost when the scratchpad goes, but the PR comments describe every scenario).
- [x] T11 — #933 backlog reader: PR #982 merged as `bf628b8`. It counts every `## Gap-backlog` table and the
  5-col shapes. gc is derived as kg − open (platform-native sync on a scratch copy gives 15/15, not 7/15). The
  denominator check handles unspaced fractions and drops only the 3011/5011 false positive. §8b is updated.
  Reader relaxations were rejected and the corpus migrated instead (niagara `571652bec`, `74b8ffdcd`, `f6e689bac`).
  Follow-ups: #967, #983.
- [x] T12 — #954 drop OpenCode: PR #978 merged as `45109ce`. Rework restored harness-sweep-parity
  and ci-path-filter-coverage, added skill-invariants.test.sh (the shared SKILL.md guards), and
  deleted the plugin machinery. The install plans for claude, codex and reasonix are identical to main (the parent
  executed the diff). Advisories are in #981.
- [x] T13 — #957 retro gate: PR #977 merged as `9ecb324`, after 3 review rounds (Opus + RDD). A retro qualifies iff
  `git diff --cached --relative --diff-filter=A <session_sha>` lists it (committed or staged, any `retros/` depth), or it is
  untracked and newer than the session file. A bad or missing session sha degrades to main's mtime behaviour. The
  session-start hook now writes its file once (compaction no longer resets it). The archive focus filter was dropped
  (doctrine first: #980). Follow-up #984. The installed Stop/SessionStart hooks (api-paneles, blender-llm) need a re-init.
- [ ] T14 — Remaining audit issues: #959 (init auto-wire), #960/#961/#962 (skill and doctrine
  docs), #963 (install.sh swallow), #965 (census), #970 (archive `--committed`), #974 (hook
  budget at 7,887/8,000).

## Acceptance
Each unit has executed RED, suite `--prove-teeth` green, shellcheck with 0 warnings, native RDD approved and
acknowledged, an Opus review addressed, CI green (all checks, including labels), and a squash merge.

## Lessons (this session)
- Four of six writer PRs had blockers that passed the suite, teeth and RDD. They were found only by the
  Opus adversarial review, which ran full-fleet sweeps and hand mutants.
- Writers claimed "pre-existing/unrelated" for a headline in-scope failure (platform-native),
  claimed RED "by code inspection", and deleted whole suites because of a harness name. Re-run and read everything.
- CI-only failures come from git `init.defaultBranch`. Reproduce them with `GIT_CONFIG_GLOBAL=/dev/null`.
- The "PR Has type:* Label" and "Issue Has status:approved" checks are not required, but they must pass.
  Label through the REST API (`gh pr edit` fails because of the Projects-classic deprecation).

## Close (2026-09-23)
- Merged: #953 #968 #952 #972 #975 #966 #978 #982 #977 (9 PRs). #969 is open and pending the final security review.
- Corpus commits: niagara-research `00f1a43fb` `571652bec` `74b8ffdcd` `f6e689bac` `6c871d054`. The ford retro
  fake row is still pending (the edit was denied: ford is not a working directory).
- Follow-ups filed: #967 #970 #971 #973 #974 #976 #979 #980 #981 #983 #984.
- The shared checkout was fast-forwarded to origin/main. The deployed skill was redeployed for claude and reasonix (drift 0; old backups
  renamed to `SKILL.md.local-backup.2026-09-22`, since they had no local deltas).
- Quiet-tree gate on merged main `9ecb324`: `run-all.sh --prove-teeth` gives 123/123 suites, 3574 cases, 0 failed
  (8 skipped; 21 suites without teeth, the known #426 list). Shellcheck: 0 warnings on 211 files.
- Worktrees and session branches were removed; only `fix/955-scan-committed-content` (PR #969) remains.

## Next step
1. PR #969: re-run the Opus security re-review, then merge.
2. Re-init the Stop/SessionStart hooks of the wired targets (api-paneles, blender-llm) to pick up the #977 write-once
   session file.
3. Remaining audit issues: #959, #960/#961/#962, #963, #965, #970, #974 (hook budget), #980 (retro-focus
   doctrine), and the follow-ups listed above.
4. ford retro: delete fake row line 19 (`corpus/retros/2026-09-10-update-notice.md`).
