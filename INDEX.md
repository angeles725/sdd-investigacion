# gentle-ai SDD — Mental Model · Master Index

**Updated**: 2026-07-28 (partial refresh — see the version-coverage warning below). Originally written 2026-06-28 (Blocks 1-27 — full distillation of gentle-ai's SDD: philosophy + DAG + 10 phases + orchestration + persistence contracts + quality + multi-agent distribution + internal Go architecture)
**System analyzed**: gentle-ai (ecosystem configurator, Go binary) + its SDD layer (Spec-Driven Development) as materialized in Claude Code / OpenCode.

> ## ⚠ THIS CORPUS IS AT TWO DIFFERENT VERSIONS — read this before trusting any block
>
> The corpus was written against **v1.43.2**. The installed binary is **v2.2.0**, a full major-version
> jump driven by **Receipt-Driven Development** (RDD), introduced in `v1.47.0` on 2026-07-10; the Go
> module moved to `/v2` because major ≥ 2 requires the `/vN` suffix.
>
> A block-by-block audit on **2026-07-28** classified all 27 blocks. Eight were STALE and have been
> refreshed. **The other nineteen have NOT been re-verified against v2.2.0.**
>
> | State | Count | Blocks |
> |---|---|---|
> | **Refreshed to v2.2.0** (2026-07-28) | 8 | 11, 12, 15, 18, 19, 22, 26, 27 |
> | **INCOMPLETE** — still true, but missing v2.2.0 surface | 11 | 1, 2, 3, 10, 14, 16, 17, 20, 21, 24, 25 |
> | **CURRENT at audit** — no drift detected, not rewritten | 8 | 4, 5, 6, 7, 8, 9, 13, 23 |
>
> **How to tell which you are reading**: a refreshed block carries a `SUBJECT VERSION` line in its
> header and uses `[DRIFTED v1.43.2→v2.2.0]` to mark facts that changed, keeping the old fact visible
> beside the new one. A block with no `SUBJECT VERSION` line has not been re-verified — treat its
> `[CERT]` markers as certified *as of v1.43.2*, not as of today.
>
> **Not yet covered by any block** `[GAP]`: the `review` command family (21 subcommands — see Block 26
> §26.2.3), `sdd-attempt`, `sdd-verify-validate` beyond its contract, and the 4R lenses as native
> sub-agents. Documenting the RDD surface in depth is deliberately deferred: upstream declares it
> unstable (*"RDD is unstable… may change while remaining issues are fixed"*, `README.md:21`) and names
> `v1.46.0` as the last release without it.
>
> **Provenance caveat**: every block's `SOURCES` header cites per-machine absolute paths, and v2.2.0
> both rewrote and RELOCATED several of those files — four sections cited from `~/.claude/CLAUDE.md`
> now live in `~/.claude/skills/_shared/sdd-orchestrator-workflow.md`. Re-verify a citation before
> relying on it.
**Method**: Empirical READ-ONLY investigation with 7 sub-agents in parallel, contrasting the orchestrator contract (`~/.claude/CLAUDE.md`) against the canonical skills (`~/.config/opencode/skills/sdd-*/SKILL.md`), the agents (`~/.claude/agents/sdd-*.md`), the prompts (`~/.config/opencode/prompts/sdd/*.md`), the shared contracts (`_shared/`), the `gentle-ai` binary (verified via `--help`), and the source repository [Gentleman-Programming/gentle-ai](https://github.com/Gentleman-Programming/gentle-ai) (README + `docs/`). Replica of the `niagara-research` corpus style.

This index guides you through the **27 blocks** that document gentle-ai's SDD. Each block is an independent `.md` file that can be read in isolation, but the connections are explicitly marked between them with the form `[Block K]`. The autogenerated flat catalog lives in [CATALOG.md](CATALOG.md) (regenerate with `python3 tools/gen-catalog.py`).

## Certainty marker legend

The whole corpus uses the same epistemic honesty system as `niagara-research`:

- **`[CERT]`** = verified by reading the primary source, with a `path:line` or `path §section` citation.
- **`[CERT-a]`** = asserted by a source (README/docs/prompt) but **not re-verified** in its primary origin (e.g. Go code, or the binary's `--help`).
- **`[INFER]`** = author's deduction, not literal in the source.

---

## Full map

### Layer 1 — Fundamentals (Blocks 1-3)

| # | Block | File | Key topics |
|---|--------|---------|------------|
| 1 | What SDD is: philosophy and the orchestrator-coordinator model | [block1](sdd-mental-model-bloque1.md) | Ecosystem configurator, thin orchestrator thread, delegation = context compression + independence of judgment, persona/artifact boundary, single delegation level |
| 2 | The phase DAG and the Result Contract | [block2](sdd-mental-model-bloque2.md) | `proposal→specs→tasks→apply→verify→archive` + `design`; Result Contract (`status`, `executive_summary`, `artifacts`, `next_recommended`, `risks`, `skill_resolution`); parallel branches converging in `tasks` |
| 3 | Artifact backends and topic keys | [block3](sdd-mental-model-bloque3.md) | `engram` / `openspec` / `hybrid` / `none`; topic keys table `sdd/{change}/{artifact}`; recovery rules; speed vs traceability tradeoff |

### Layer 2 — The 10 phases (Blocks 4-13)

| # | Block | File | Key topics |
|---|--------|---------|------------|
| 4 | `sdd-init` phase | [block4](sdd-mental-model-bloque4.md) | Stack + testing capabilities detection, skill-registry scan, Strict TDD resolution, Decision Gates per mode |
| 5 | `sdd-explore` phase | [block5](sdd-mental-model-bloque5.md) | Read-only, standalone vs tied to a change, the only phase with `WebFetch`/`WebSearch`, writes `explore` |
| 6 | `sdd-propose` phase | [block6](sdd-mental-model-bloque6.md) | Product question round (10 axes), Capabilities section as a contract with spec, opus model |
| 7 | `sdd-spec` phase | [block7](sdd-mental-model-bloque7.md) | Delta specs, `MODIFIED` copy-full-then-edit workflow, RFC 2119, testable scenarios |
| 8 | `sdd-design` phase | [block8](sdd-mental-model-bloque8.md) | Reads proposal → ADR decisions with rationale + rejected alternatives, Testing Strategy, opus model |
| 9 | `sdd-tasks` phase (+ Review Workload Forecast) | [block9](sdd-mental-model-bloque9.md) | Mechanical breakdown, line estimation, chained PRs recommendation, no own `commands/` |
| 10 | `sdd-apply` phase (+ apply-progress continuity) | [block10](sdd-mental-model-bloque10.md) | Implementation, read-merge-write of apply-progress across batches, strict TDD forwarding |
| 11 | `sdd-verify` phase (+ report format) | [block11](sdd-mental-model-bloque11.md) | **v2.2.0** · CRITICAL / WARNING / SUGGESTION levels, mechanical verdict, Assertion Quality Audit · new `sdd-verify-validate` admission gate before ANY write (canonical description in [Block 19] §19.10), new YAML envelope, early-exit `blocked` on pending tasks `[DRIFTED]` · the old prompt-vs-skill CRITICAL DIVERGENCE is resolved, but the files still differ: the prompt is the skill **plus** an injected `codegraph-guidance` section |
| 12 | `sdd-archive` phase | [block12](sdd-mental-model-bloque12.md) | **v2.2.0** · Merge of delta specs into main specs, cycle closure, haiku model, no `Bash` · new Native Review Receipt Gate (`reviewGate.result: allow`, no override) §12.5a · **§12.5b documents an upstream-admitted INCONSISTENCY: the native gate defers but the skill contract still blocks (`README.md:218`)** — a moving target, not settled behaviour |
| 13 | `sdd-onboard` phase | [block13](sdd-mental-model-bloque13.md) | Guided pedagogical walkthrough, runs the behavior of all phases inline, haiku model |

### Layer 3 — Orchestration (Blocks 14-18)

| # | Block | File | Key topics |
|---|--------|---------|------------|
| 14 | Meta-commands: `sdd-new`, `sdd-continue`, `sdd-ff` | [block14](sdd-mental-model-bloque14.md) | Meta-command vs skill, planning fast-forward, which stretch of the DAG each one covers |
| 15 | `sdd-status` and the native `gentle-ai` dispatcher | [block15](sdd-mental-model-bloque15.md) | **v2.2.0** · Dispatcher only sees OpenSpec (`artifactStore: openspec`), blind to engram, routing by `nextRecommended`/`blockedReasons`, Action Context Guard · new §15.5a **`sdd-attempt` is a SECOND routing authority** that must be queried separately before runtime-bearing continuations · §15.2a: the no-per-subcommand-`--help` claim **re-confirmed** at v2.2.0, extended with three distinct parser behaviours |
| 16 | Execution modes (auto/interactive) and the Gatekeeper | [block16](sdd-mental-model-bloque16.md) | Auto vs interactive, 5 Gatekeeper checks (conformance/existence/no-hallucination/no-drift/routing), inline vs fresh reviewer, single re-run |
| 17 | Delivery strategy, chain strategy, and chained PRs | [block17](sdd-mental-model-bloque17.md) | `ask-on-risk`/`auto-chain`/`single-pr`/`exception-ok`, `stacked-to-main`/`feature-branch-chain`, Review Workload Guard, 400-line budget |
| 18 | Delegation model, mandatory triggers, and model assignments | [block18](sdd-mental-model-bloque18.md) | **v2.2.0** · Direct-inline vs delegated-worker table, phase→model→effort→reason table, model gate, launch dedup · **the trigger set was REBUILT** `[DRIFTED]`: 2 survive (4-file, Write), 4 removed (PR, Incident, Long-session, Fresh review), 4 new (Bounded read, Context, Per-action, Optional SDD) · §18.4a: the 5 RDD review-lens sub-agents — **assigned in OpenCode's `model_assignments` but ABSENT from `claude_phase_assignments`** `[GAP]` · doctrine is machine-local, not distributed `[GAP]` |

### Layer 4 — Contracts and conventions (Blocks 19-22)

| # | Block | File | Key topics |
|---|--------|---------|------------|
| 19 | Persistence contract (`persistence-contract`) | [block19](sdd-mental-model-bloque19.md) | **v2.2.0** · Mode is a session property, default `none` when in doubt, prompt capture asymmetry, recovery · **§19.10 is the CANONICAL description of the `sdd-verify-validate` admission gate** (Blocks 11 and 22 cross-reference it): byte-exact candidate, zero writes on denial or unavailability, hybrid preflight once |
| 20 | Engram convention (`engram-convention`) | [block20](sdd-mental-model-bloque20.md) | Topic key format, `mem_save`/`mem_search`/`mem_get_observation`, upsert by `topic_key`, `active`/`needs_review` lifecycle |
| 21 | OpenSpec convention (`openspec-convention`) | [block21](sdd-mental-model-bloque21.md) | `openspec/changes/`, `state.yaml`, source vs delta specs, the `MODIFIED` trap, immutable dated archive |
| 22 | Skill-resolver + phase-common + status-contract | [block22](sdd-mental-model-bloque22.md) | **v2.2.0** · Skill Resolver Protocol (pass paths not summaries), common phase contract, structured status schema · `sdd-status-contract.md` grew 124 → 160 lines `[DRIFTED]`: new fields `remediationState`/`reviewGate`/`reviewTransaction`, new `nextRecommended` values `review`/`remediate`/`resolve-review`, the `sdd-attempt` gate, and receipt-matching on the `archive` dependency state |

### Layer 5 — Quality (Blocks 23-24)

| # | Block | File | Key topics |
|---|--------|---------|------------|
| 23 | Strict TDD (strict-tdd / strict-tdd-verify) | [block23](sdd-mental-model-bloque23.md) | The Three Laws, activation via init, mandatory forwarding to apply/verify, Safety-Net→RED→GREEN→TRIANGULATE→REFACTOR cycle, Assertion Quality Audit |
| 24 | judgment-day and adversarial verification | [block24](sdd-mental-model-bloque24.md) | Blind dual judge (jd-judge-a/b) + jd-fix-agent, synthesis into buckets, mandatory re-judgment, post-design/apply trigger, cost ~4 + 3×findings |

### Layer 6 — Distribution and configurator (Blocks 25-26)

| # | Block | File | Key topics |
|---|--------|---------|------------|
| 25 | Multi-agent distribution: the 15 harnesses and the delegation models | [block25](sdd-mental-model-bloque25.md) | Table of 15 agents; **Full** vs **Solo-agent** vs **Detect-only**; per-harness paths (`~/.cursor/agents/`, `~/.kiro/agents/`, etc.); invariant SDD phases, variable context transport |
| 26 | The `gentle-ai` configurator | [block26](sdd-mental-model-bloque26.md) | **v2.2.0** · `install`/`sync`/`upgrade`/`restore`/`doctor`/`skill-registry refresh`+`list`, global vs workspace scopes, OpenCode SDD profiles, per-phase models, backups, startup hooks, RDD version policy (Minisign signing, `v1.46.0` stable pin) · **the top-level `--help` under-reports `review` 3× — 7 advertised vs 21 real (§26.2.3)** · Go 1.25.10+, Scoop suspended `[DRIFTED]` |
| 27 | Internal architecture: `state.json` and per-harness adapters | [block27](sdd-mental-model-bloque27.md) | **v2.2.0** · `InstallState` **9 → 16+ fields** `[DRIFTED]` (`selection_configured`, `components`, `preset`, `sdd_mode`, `community_tools`, `codexOrchestratorAssignment`), `model_assignments` 19 keys incl. 5 new `review-*`, new `~/.gentle-ai/review-contexts/v1/` (§27.8), 16 adapters, `SystemPromptStrategy`/`MCPStrategy` · Go source claims downgraded to `[CERT-a]` (the Cellar ships no source) |

---

## Cross-cutting findings (verified during the investigation)

These are the non-obvious discoveries that emerged when contrasting sources. They serve as a map of the **system's inconsistencies**, not as criticism — they document where the source of truth diverges:

1. **The ASCII DAG contradicts the dependencies table** `[CERT]` (B2). The diagram draws `design → specs`, but the phases table says `sdd-tasks` reads **spec + design** and `sdd-design` reads **proposal**. Operationally they are parallel branches from `proposal` that converge in `tasks`; the ASCII compresses that confluence.

2. **Vocabulary discrepancy in `status`** `[CERT]` (B4-B8). The agent prompts declare `done|blocked|partial`, but the common Return Envelope (`sdd-phase-common.md §D`) uses `success|partial|blocked`.

3. **The condensed prompts contradict their SKILL.md in `apply` and `verify`** `[CERT]` (B10-B11). The strongest case: `prompts/sdd/sdd-verify.md:33` says *"Do NOT run tests unless strict_tdd is active"*, the OPPOSITE of the SKILL.md (*"static analysis alone is never verification"*). The SKILL.md v3.0 + `references/report-format.md` is the canonical source. For tasks/archive/onboard the prompts are byte-identical to the SKILL.md.

4. **The native dispatcher does not expose `--help` per subcommand** `[CERT]` (B15). `gentle-ai sdd-status --help` fails with `unknown sdd-status argument "--help"`. The flags `--cwd`/`--json`/`--instructions` remain `[CERT-a]` (asserted in docs, not verifiable against the v1.43.2 binary).

5. **Hermes: `Detect-only` vs `Full`** `[CERT]` (B25). The README labels Hermes as `Detect-only`; `docs/agents.md` labels it `Full (delegate_task ephemeral)`. They are distinct axes `[INFER]`: `Detect-only` = installation mode; `Full` = runtime delegation mode. The README table lists 16 rows; Hermes is the 16th outside the 15 that the configurator installs automatically.

6. **opus ↔ Gatekeeper high-risk coincidence** `[CERT]` (B16, B18). The only two phases with the **opus** model (`sdd-propose`, `sdd-design`) are exactly the two high-risk phases that the Gatekeeper validates with a fresh reviewer. The model assignment is proportional to architectural risk.

7. **`docs/architecture.md` lists 8 Go adapters but the code registers 16** `[CERT]` (B27, resolved). `internal/agents/factory.go:defaultAgentIDs` registers **16 adapters** (claude, opencode, kilocode, gemini, cursor, vscode, codex, antigravity, windsurf, kimi, qwen, kiro, openclaw, pi, trae, hermes), each with `internal/agents/<pkg>/adapter.go` + test. The "8" in `docs/architecture.md` is doc debt (MVP snapshot); the "15" in the README is presentational (Hermes is the 16th row, detect-only). The authoritative count is **16**. Only 4 declare `SupportsSubAgents()==true`: Claude, Cursor, Kimi, Kiro.

8. **`state.json` is configurator state, not SDD state** `[CERT]` (B27). The `InstallState` struct (`internal/state/state.go`) persists `installed_agents`, per-agent/phase model assignments, `persona`, `last_update_check`, `pending_sync` — but does **NOT** persist SDD tasks/specs or scope/channel. The state of the SDD changes lives in the artifact backends ([Block 3]), not here.

---

## Pending verification (honest gaps)

- Native dispatcher flags (`--cwd`/`--json`/`--instructions`): not verifiable via the v1.43.2 binary's `--help` (asserted in docs, `[CERT-a]`).
- Body of the injectors (`components/sdd/inject.go`, `filemerge/`): B27 documents the contract (paths + per-adapter strategies), not the exact bytes each injector writes.

---

## How to navigate this corpus

- **If you want to understand SDD from scratch** → start with [Block 1] (philosophy), continue with [Block 2] (DAG) and [Block 3] (backends).
- **If you are interested in a specific phase** → go directly to its block in Layer 2 (B4-B13).
- **If you want to know how the orchestrator makes decisions** → Layer 3 (B14-B18), especially [Block 16] (Gatekeeper) and [Block 18] (delegation + models).
- **If you are going to touch persistence or backends** → Layer 4 (B19-B22).
- **If you are interested in how it is distributed to your agent (Claude/Codex/OpenCode/etc.)** → [Block 25] and [Block 26].
